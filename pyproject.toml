[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "myfirstagent"
version = "0.1.0"
description = "A simple AI agent project"
authors = [{name = "You", email = "you@example.com"}]
readme = "README.md"
requires-python = ">=3.9"
